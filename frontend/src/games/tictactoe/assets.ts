import type { GameAsset } from '@/lib/assets'

/** @package */
export const tictactoeAssets: GameAsset[] = [
  // No external assets yet — TicTacToe is rendered with pure CSS/SVG.
  // Add entries here when sounds or images are introduced.
]
