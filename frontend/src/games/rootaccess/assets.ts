import type { GameAsset } from '@/lib/assets'

export const rootAccessAssets: GameAsset[] = [
  // No external assets yet — Root Access uses inline SVG card art.
  // Add entries here when card images or sounds are introduced.
]
