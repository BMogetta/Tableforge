{
  "mcpServers": {
    "grafana": {
      "command": "/home/bruno/.local/bin/uvx",
      "args": ["mcp-grafana"],
      "env": {
        "GRAFANA_URL": "http://localhost/grafana",
        "GRAFANA_SERVICE_ACCOUNT_TOKEN": "${GRAFANA_MCP_TOKEN}"
      }
    },
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://recess:recess@localhost:5432/recess"
      ]
    },
    "recess-game": {
      "command": "npx",
      "args": ["--yes", "tsx", "./tools/mcp-game/src/index.ts"],
      "env": {
        "RECESS_BASE_URL": "http://localhost"
      }
    }
  }
}
